import React from "react"
import style from "./DateFilter.module.scss"

type DataFilterPropsType = {}

export const DataFilter = React.memo((props: DataFilterPropsType) => {
    return (
        <>

        </>
    )
})